using System;

namespace Connect4App
{
    class Program
    {
        static void Main(string[] args)
        {
            /*Menu m = new Menu();
            m.start();
            get input for board and choose opponent types p1, p2,...
            choose symbol
            Menu.decideReihenfolge(){shuffle playerlist)
            while(!Board.won){
                foreach player in playerlist:
                    Board.getInput(player.getRow(7))
                if(Board.won){print Board.victorious}
           
        }
        */
        }
    }
}
